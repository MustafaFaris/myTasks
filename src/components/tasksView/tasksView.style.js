import styled from "@emotion/styled";

const StyledTasksView = styled.div`
  padding: 15px;
  max-width: 1140px;
  margin: auto;

  .task {
    margin-bottom: 10px;
  }

  .no-tasks {
    margin: 60px 0;
    text-align: center;
    color: #888;
  }
`;

export default StyledTasksView;
